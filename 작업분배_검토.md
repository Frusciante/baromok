# 바로목 마무리 작업 — 파트 분배 & 필요성 검토

분배 기준: **마루**는 Claude로 폴더(전체 코드·데이터) 참조가 가능 → 여러 파일에 걸친 수정·리팩터·최적화·데이터 분석·빌드/테스트가 필요한 작업. **친구**는 AI는 쓰지만 폴더 참조 불가 → 판단/조사/설계 작업이나, 파일 1개만 복붙하면 되는 국소적 작업에 적합.

코드를 직접 확인한 결과 일부 작업은 **이미 구현되어 있거나, 굳이 필요 없거나, 오히려 안 하는 게 나은** 항목들이 있어 아래에 함께 표시했습니다.

---

## 🟣 마루(Claude) 담당 권장 — 폴더 참조가 꼭 필요한 작업

| 작업 | 이유 (왜 Claude) | 필요성/우려 |
|---|---|---|
| **기댄자세 → 거북목 오인식 수정** | `judgment_engine_v2.py` + `calibration_v2.py` + `debug_logs`·`sessions` 실측 데이터까지 같이 봐야 함 | **필요 (높음)**. 현재 recline과 forward_head_only가 둘 다 `cheek_distance` 증가(+Δ%)를 쓰고 **크기 차이로만** 구분함. 기댄 자세는 보통 얼굴이 멀어져 cheek가 **감소**해야 맞을 가능성이 큼 → 로직 자체를 데이터로 재검증해야 함 |
| **모든 사용자 기본 임계치 찾기 + 범위 정의** | `data/sessions`, `debug_logs`의 다중 사용자 실측치를 전부 읽어 분포를 봐야 함 | **필요하되 주의**. "모두에게 맞는 단일 기본값"은 사실상 불가능(그래서 캘리브레이션이 존재). → "합리적 기본값 + 캘리브레이션 유지"로 목표를 잡는 게 맞음 |
| **landmark_extractor 최적화** | 매 프레임 face·pose·hand **3개 모델을 전부** 돌림. 파이프라인 전체 프로파일링 필요 | **필요 (높음, 효과 큼)**. hand/pose를 저빈도로 돌리기·미사용 모델 제거·프레임 리사이즈 등으로 큰 개선 여지. 우려: 빈도 낮추면 chin_rest 등 일부 판정 반응성 저하 → 검증 필요 |
| **1시간+ 세션 통계 누락 버그** | 재현 + 대용량 세션 json 분석이 필요 | **필요 (버그)**. 장시간 세션은 frame_records가 수만 개 → 저장 실패/로드 누락 의심. 데이터로 원인 추적해야 함 |
| **세션 json → DB(SQLite) 저장** | `session_manager.py` 구조 변경 + 기존 json 마이그레이션 | **재검토 권장**. 지금 json으로도 동작함. 마감 임박이라면 우선순위 낮음. "DB가 더 전문적으로 보임" 정도 이유라면 SQLite는 중간 공수 — 1시간+ 버그 수정이 더 급함 |
| **알람 팝업 중앙 기준 수정** | `app.py`의 `popup_position_xy`가 `main_window.geometry()`(프로그램 창) 기준 → **모니터 화면 기준**(`screen().availableGeometry()`)으로 바꾸면 됨 | **필요 (간단)**. 원인·해결 위치 확인 완료. Claude면 금방. (국소적이라 친구도 가능) |
| **나쁜자세 전이 vs 유지 로직 수정** *(우성 표시)* | `state_machine.py`에 집중되어 있으나 `judgment_engine`과 의미 연동 | **필요(우선도 낮음)**. 1개 파일 위주라 친구도 가능하지만, 원하는 동작 정의가 먼저 명확해야 함 |
| **지속 캘리브레이션 구현** (만약 하기로 한다면) | `calibration_v2.py`+엔진 다중 파일 | **하지 말 것 권장** (아래 우려 참고) |

## 🔵 친구(AI·폴더X) 담당 권장 — 판단/조사/콘텐츠/국소 작업

| 작업 | 이유 (왜 친구 가능) | 필요성/우려 |
|---|---|---|
| **얼굴 기울임/돌림 감도 추가 여부 결정** | 순수 판단 — 코드 불필요 | **불필요에 동의**. head_tilt는 이미 자세로 감지되고, turned_head는 상태머신에서 경고 유지로 처리됨. 설정 항목만 늘고 이점 적음 → **추가 안 함** |
| **초기자세설정 가이드 자세히 제공** *(은수 표시)* | 대부분 **안내 문구 작성**(콘텐츠) + 약간의 UI. 문구는 AI로 충분히 초안 가능 | **필요**. 사용자가 올바른 기준자세를 잡게 하는 핵심. 친구가 문구 초안 → 마루가 `baseline_screen.py`에 반영하는 분업도 좋음 |
| **초기자세 그래프 삭제** | `baseline_screen.py`의 CalibrationScatterChart 제거 — 국소적 | **재검토**. 정말 불필요한지 먼저 합의. 삭제 자체는 작지만 참조 2곳 정리 필요 → 친구가 파일 복붙해 처리 가능 |
| **배포(패키징) 방법 조사** | 현재 패키징 설정 전무. PyInstaller 등 **방법 조사·결정**은 폴더 없이 가능 | **필요**. 친구가 방식·옵션 조사 → 실제 빌드/테스트(에셋·모델 경로 포함)는 마루가 폴더에서 수행하는 분업 권장 |

---

## ⚠️ 진짜 필요한지 다시 볼 항목 (우려 종합)

1. **같은 날 세션 합치기 — 이미 구현됨.** `statistics_screen.py`의 `DateAggregator`가 같은 날짜 라벨 세션을 합산함. **새 작업이 아니라 "정상 동작 검증"으로 축소** 가능. (단, 최근 10개만 로드한 뒤 합치므로 "오래된 같은 날"이 잘릴 수 있음 — 이 점만 확인)
2. **지속 캘리브레이션 — 권장하지 않음.** 자세 교정 앱에서 실시간 재보정은 나쁜 자세를 새 기준(neutral)으로 학습해버려 **교정 목적을 무너뜨릴 위험**이 큼. 굳이 한다면 "장시간 양호 상태에서만, 아주 느리게" 같은 강한 제약 필요. 마감 대비 효용 낮음.
3. **json → DB** 와 **단일 기본 임계치**는 "기대만큼 효과/필요"가 크지 않을 수 있어 마감 우선순위에서 뒤로.
4. 반대로 **꼭 해야 하는 것**: 기댄자세 오인식, 1시간+ 통계 버그, landmark 최적화, 배포 — 데모/제출 품질에 직접 영향.

## 제안 분담 요약
- **마루(Claude)**: 기댄자세 오인식, 기본 임계치 분석, landmark 최적화, 1시간+ 버그, 배포 빌드, 팝업 위치 (+ DB는 시간 남으면)
- **친구(AI)**: 감도 추가 여부 결정(=안 함), 가이드 문구 작성, 그래프 삭제 여부 판단, 배포 방식 조사, (여력 시) 전이/유지 로직
