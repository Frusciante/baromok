# 작업 정리 및 인계 문서 (2026-06-08)

- **브랜치**: `junha2`
- **작성**: 마루
- **범위**: 자세 판정 로직 정합성 / 알림·라벨 / 세션 저장·통계 / 허브 화면

---

## 1. 오늘 완료한 작업

### 1-A. 이미 커밋됨 — `46f1f8e`
> "자세 라벨·알림 문구 단일화 및 감도/거리 임계 정합"

- **감도 기본값 단일화**: `config.py` / `settings_widgets.py` / `judgment_engine.py`에 제각각이던 기본값(0.075/0.01 vs 0.1/0.04)을 **환경설정 경로 기준 0.1 / 0.04로 통일**. 런타임 값은 `data/config.json`의 사용자 저장값을 사용하므로 동작 변화는 없고, fallback 불일치만 제거.
- **eye_close 거리 임계 정합**: fallback 40cm → **45cm**(config 값과 일치)로 통일.
- **자세 이름·알림 문구 config 단일 소스화**:
  - 자세 한글 이름이 4곳(`camera_worker` / `detection_screen` / `chart_widgets` / config)에 흩어져 서로 다르던 것을 **config(`posture_definition_criteria.json`) 단일 소스**로 통합. `config.get_posture_label()` / `get_posture_alert_message()` 헬퍼 추가, 나머지는 헬퍼 호출로 교체.
  - `display_label`에서 "경향/의심" 제거(깔끔한 명사화), 불확실성은 알림 문장 어투로 이동.
  - 알림 문구를 config의 `alert_message` 필드로 통일(자세별 1문장), eye_close 중복 문장 제거.

### 1-B. 미커밋 (working tree) — **커밋 필요**

| 수정 | 파일 | 내용 |
|---|---|---|
| 앱 종료 시 세션 마감 | `src/ui/app.py` | 종료 경로에서 `current_session`이 있으면 `end_session()` 호출. 창 닫기/앱 종료 시 세션이 `end_time=NULL`로 남아 사라지던 문제 해결 |
| 프레임 기록 UI 분리 | `src/ui/screens/detection_screen.py` | `_on_frame_processed`에서 `add_frame_data`를 **독립 try로 분리하고 UI 렌더링보다 먼저** 실행. 렌더링 예외가 세션 기록을 막지 않도록 |
| 통계 state 대소문자 수정 | `src/core/session_manager.py` | `_calculate_stats_from_db`의 state 비교를 `UPPER(state)`로 변경. (저장값은 소문자 `normal/warning/bad_posture`인데 SQL은 대문자 비교 → good/warn/bad가 **항상 0** → 허브가 무조건 "0점") |
| 통계 분포 누락 보완 | `src/core/session_manager.py` | `posture_distribution`에 **side_tilt, turned_head 추가**(eye_close는 재작업 전이라 제외) |
| 허브 실시간 갱신 | `src/ui/screens/hub_screen.py` | `showEvent` 추가 — 허브 진입 시마다 세션 통계를 다시 읽어 재구성. 감지 종료 후 복귀 시 점수 갱신됨 |

> **추천 커밋 메시지**: `세션 저장/통계 집계 버그 수정 및 허브 실시간 갱신`
> (또는 분리: ① `종료 시 세션 저장 누락 수정 + 프레임 기록을 UI 예외와 분리` ② `통계 state 대소문자/분포 누락 수정 + 허브 진입 시 갱신`)

---

## 2. 진단했지만 코드는 안 고친 것 (근본 원인 / 차기 과제)

### eye_close 과탐 — **seungbin님께 인계**
- eye_close("화면 가까움")는 **홍채 기반 절대거리(cm)** 로 판정하는데, 카메라 화각(HFOV 60° 가정)·`−20cm` 보정 등 매직넘버 때문에 평범한 거리(50~60cm)에서도 과탐. 바른 자세를 "화면 가까움/나쁜 자세"로 잘못 잡음.
- eye_close는 seungbin님(`Frusciante/hsbin0531`)이 6/2에 추가(`b39bd37`, `cb97d78`)한 본인 기능 → **본인이 상대비율 방식으로 재작업** 권장.
- **권장 방향**(설계결정 문서 4장 참고): 절대 cm 대신 **baseline 대비 홍채 지름 비율**로 전환. baseline 촬영 때 사용자 평소 홍채 지름(px) median을 기록 → 현재 지름이 N% 커지면(=가까워지면) 트리거. 홍채_mm·HFOV가 약분돼 카메라 보정 불필요.

### 자세 로직 담당자용 (차기 과제)
- **side_tilt가 recline을 가림**: 눈 기울기 12~27° 구간에서 recline 가드가 차단되고 side_tilt만 살아남음. recline/forward의 기울기 가드를 하드 차단(0) → soft 감쇠로, 깊이 기반 자세를 tilt보다 우선하도록.
- **recline 바른자세 오탐**: deviation이 어깨너비(RANSAC)에 의존 → 어깨 노이즈로 광대 예상값이 흔들려 오탐.
- **임계 자동 보정**: 사용자 수동 +/- 대신 baseline 노이즈(mean/max_inlier_deviation) 기반 자동 산출 + 둔감/보통/민감 프리셋.

---

## 3. 마루(본인) 할 일

- **git 정리**: 현재 `.git/worktrees`에 옛 에이전트 worktree가 남아 `git status`가 에러남(샌드박스 권한으로 삭제 불가). **윈도우에서** `git worktree prune` 실행, 남으면 `.claude/worktrees` 및 `.git/worktrees/*` 삭제 → 정상화.
- **1-B 미커밋 변경 커밋** (위 추천 메시지 참고).
- **노트북에서 동작 검증**: 감지→종료→허브 복귀 시 ①세션이 통계에 뜨는지 ②허브 점수 갱신되는지 ③0점이 아닌 정상 통계가 나오는지.

---

## 4. 주의사항 / 참고

- **0프레임 세션 근본 원인**: 본체 PC는 카메라가 없어 0프레임이 정상(진단 불가). 노트북 데이터로 확인한 결과 **프레임 기록 자체는 정상**(704/257/155프레임 등 기록됨)이었고, 문제는 ①통계 state 대소문자 버그(0점) ②앱 닫기 시 미마감이었음 — 둘 다 1-B에서 수정됨.
- **기존 DB 세션은 여전히 0점**: 이미 저장된 잘못된 통계는 자동으로 안 고쳐짐. 최종 테스트 때 DB 삭제 예정이라 **마이그레이션은 생략**.
- **eye_close는 통계 분포에서 제외**됨(재작업 전 노이즈 방지). 단 eye_close 트리거 프레임은 여전히 state(warning/bad)에는 반영되므로, 완전한 정합은 seungbin님 재작업 후.
- **참고 문서**: `자세로직_설계결정요약.docx`(설계 판단·근거 정리).

---

## 5. 한눈에 보기 — 누가 무엇을

| 담당 | 할 일 |
|---|---|
| **마루** | git 정리 → 미커밋 변경 커밋 → 노트북 동작 검증 |
| **seungbin** | eye_close 절대cm → baseline 상대 홍채비율 재작업 |
| **자세로직 담당** | side_tilt↔recline 가림 / recline 오탐 / 임계 자동보정 |
